@using REST_API.Models
@model IEnumerable<Question>
@{
    ViewData["Title"] = "Question";
}
<div class="container justify-content-center mt-5 ">
    <div class=" row align-content-center bg-opacity-10 bg-info mt-8 pt-3 mb-3 offset-md-1">
        <h4 class="text-dark  text-center text-lg-center text-uppercase"> View ALL Question</h4>
    </div>
    <p> <a asp-action="Create" class="btn btn-primary btn-md " role="button">Create New</a> </p>
    <table class="table table-bordered  table-hover text-center table-striped d-xxl-table-cell">
        <thead style="background-color: lightgreen">
            <tr class="mt-3 pt-3">
                <th> </th>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.QuestionText)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ArticleId)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a asp-action="Update" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="GetQuestion" asp-route-id="@item.Id">Details</a>
                        @* <a asp-action="Delete" asp-route-id="@item.ArticleId">Delete</a>   *@

                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        <span>@Html.Raw(item.QuestionText)</span>
                        @*  @Html.DisplayFor(modelItem => item.QuestionText) *@
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ArticleId)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>